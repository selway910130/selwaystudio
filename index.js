var srcLst=new Array;window.addEventListener('load', function() {	console.log("loaded successfully");	var oReq = new XMLHttpRequest();	oReq.onload = function() {		createView(this.response.split('; '));	};		oReq.open("get", "index.php", true);	oReq.send();});function createView(lst) {	//  Let newest photo show first	lst.reverse();	var body = document.getElementById('body');	for (var i = 1; i < lst.length - 1; i++) {		var photoDiv = document.createElement('div');		photoDiv.className = 'photoContainer';		photoDiv.style.backgroundImage = 'url(' + lst[i] + ')';						//var photo = document.createElement('img');		//photo.src = lst[i];		//photo.className = 'photo';						//photoDiv.appendChild(photo);		body.appendChild(photoDiv);			}}